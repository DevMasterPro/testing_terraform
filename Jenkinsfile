pipeline {
    agent none
    node {
        stages {
            stage('terraform version') {
                steps {
                    sh "hostname"
                }
            }

        }
    }
}
